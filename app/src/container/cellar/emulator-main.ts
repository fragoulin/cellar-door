import { connect } from 'react-redux'
import EmulatorMain from 'components/cellar/emulator-main/emulator-main'

/**
 * Connect this container to the component.
 */
export default connect()(EmulatorMain)
